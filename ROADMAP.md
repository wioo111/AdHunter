# AdHunter 开发规划

## 产品边界

AdHunter 的核心不是“展示几张广告截图”，而是把识别软广做成一个可重复游玩的判断游戏。近期所有开发都应服务三个指标：关卡生产效率、命中判定可信度、玩家完成一局的流畅度。

## P0：稳定可维护（本 PR）

- 修复构建脚本错误和兜底数据异步竞态。
- 校验云端与本地关卡数据，拒绝无效坐标、重复 ID 和空关卡。
- 修复重复点击已找到热点被判错、弹窗计时器互相覆盖、图片失败无反馈等问题。
- 完成可用的本地标注器，并支持导出完整关卡 JSON。
- 建立 README、环境变量示例和 GitHub Actions 构建检查。

验收标准：`npm run check` 通过；云端失败时仍可稳定进入游戏；标注者无需手写坐标即可产出关卡 JSON。

## P1：关卡生产闭环

1. 建立受保护的管理端，而不是继续在玩家页面塞隐藏功能。
2. 管理端支持图片上传、热点编辑、关卡预览、草稿和发布状态。
3. 后端提供明确的读取与写入接口；前端不再拼接数据库查询语句。
4. 使用单一数据源生成本地兜底文件，消除 `levels_schema.json` 与 `fallback_data.json` 人工双写。
5. 为关卡数据增加 schema 版本和迁移脚本。

验收标准：编辑、审核、发布一关不需要修改仓库源码；线上数据和离线兜底可由同一命令生成。

## P2：游戏体验与可观测性

1. 增加首页、关卡选择、结算页和进度持久化。
2. 对移动端触摸区域、不同长宽比图片和重叠热点做专门适配。
3. 增加错误点击次数、完成时长、热点命中率和关卡流失率埋点。
4. 根据真实数据调整热点半径和关卡难度，而不是凭感觉修改。
5. 补充单元测试和端到端测试，覆盖数据校验、命中算法和通关流程。

验收标准：能够回答“哪一关最难、哪个热点误点最多、玩家在哪退出”，并据此迭代关卡。

## 暂不做

- 不做 AI 自动识别软广。当前数据规模和产品闭环不足，先把人工出题与玩家验证跑通。
- 不做复杂账号、排行榜和社交系统。它们不会解决关卡供给与判定质量问题。
- 不继续扩展散落在前端的 CloudBase 直连逻辑。下一次后端改造必须收敛成明确 API。
